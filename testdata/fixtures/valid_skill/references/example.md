# Example Reference

This is a sample reference document for the test skill.

## Overview

This document demonstrates the reference file structure used in skills.

## API

```rust
fn example() -> Result<(), Error> {
    Ok(())
}
```
